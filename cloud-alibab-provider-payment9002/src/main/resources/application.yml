server:
  port: 9002

spring:
  application:
    name: nacos-provider
  #配置nacos 服务地址
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
#暴露端点
management:
  endpoints:
    web:
      exposure:
        include: "*"
